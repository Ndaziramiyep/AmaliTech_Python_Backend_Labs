"""Superseded by tests/unit/cli/test_commands.py, which covers every scriptable subcommand
(including follow-user/unfollow-user) against the consolidated `commands.py` CLI.

Delete this file whenever convenient -- it collects zero tests and is kept only because
bulk deletion was blocked during the project restructure.
"""

from __future__ import annotations
