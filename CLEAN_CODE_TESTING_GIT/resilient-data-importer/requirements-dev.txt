# Development / tooling dependencies (testing, linting, formatting, git hooks).
-r requirements.txt

pytest>=8.0
pytest-mock>=3.14
pytest-cov>=5.0
coverage[toml]>=7.5

black>=24.0
ruff>=0.6
mypy>=1.10

pre-commit>=3.7
