from django.apps import AppConfig


class UrlShortenerConfig(AppConfig):
    """Django app configuration for the url_shortener app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "url_shortener"
