# Credentials for the shared Postgres container defined in the root
# docker-compose.yml (used by pgAdmin / any client connecting to
# localhost:5432 directly, and read into that container by env_file).
# Must be copied to `.env` — real `.env` files are gitignored.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=patrick
POSTGRES_DB=postgres
