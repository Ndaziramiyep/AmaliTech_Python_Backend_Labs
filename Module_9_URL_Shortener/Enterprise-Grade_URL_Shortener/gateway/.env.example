# Must match auth-service's INTERNAL_SERVICE_TOKEN — sent as X-Internal-Token
# when the gateway calls auth-service's internal token-validation endpoint
# (see server.conf's /internal/verify) so it can tell the call came from the
# gateway and not the public internet.
INTERNAL_SERVICE_TOKEN=change-me-shared-gateway-token
